pages:
    stage: deploy
    image: alpine:3.22.4
    before_script:
        - apk add --no-cache pandoc
    script:
        - sh scripts/build-pages.sh
    artifacts:
        paths:
            - public
    only:
        - gh-pages
