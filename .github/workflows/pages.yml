# 设置环境变量以强制 GitHub Actions 使用 Node.js 24, 消除 Node.js 20 弃用警告;
# 参考: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
name: GitHub Pages

on:
    push:
        branches:
            - gh-pages

env:
    FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
    build:
        runs-on: ubuntu-latest
        container:
            image: alpine:3.22.4
        steps:
            - uses: actions/checkout@v4

            - name: Install pandoc
              run: apk add --no-cache pandoc

            - name: Build pages with shared script
              run: sh scripts/build-pages.sh

            - name: Upload Pages Artifact
              uses: actions/upload-pages-artifact@v3
              with:
                  path: ./public

    deploy:
        needs: build
        permissions:
            pages: write
            id-token: write
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}
        runs-on: ubuntu-latest
        steps:
            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v4
